version: 1.0
runtime: python3
build:
  commands:
    build:
      - apt-get update && apt-get install -y libpq-dev
      - pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: sh startup.sh
  network:
    port: 8000
