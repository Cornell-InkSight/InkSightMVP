version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3.11 -m ensurepip --upgrade
      - pip3 install -r requirements.txt
run:
  runtime-version: 3.8
  command: sh startup.sh
  network:
    port: 8000
